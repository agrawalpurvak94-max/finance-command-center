/**
 * Statements (Module 5) have not been implemented yet — no Stitch design or
 * Supabase view shape (`vw_statements`, `vw_statement_summary`, etc., per
 * CLAUDE.md) has been approved. This file is an intentionally empty
 * placeholder so that when that module starts, its types have one stable
 * home from the beginning instead of being invented ad hoc inside
 * components. Do not add speculative interfaces here before Module 5 is
 * scoped — see ARCHITECTURE_AUDIT.md.
 */
export {}
